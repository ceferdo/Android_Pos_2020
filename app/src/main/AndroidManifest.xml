<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aula.pos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pos2020">
        <activity
            android:name=".LinhaListaActivity"
            android:label="@string/title_activity_linha_lista"
            android:theme="@style/Theme.Pos2020.NoActionBar"></activity>
        <activity
            android:name=".PaisesActivity"
            android:label="@string/title_activity_paises"
            android:theme="@style/Theme.Pos2020.NoActionBar" />
        <activity android:name=".ListaActivity" />
        <activity
            android:name=".ResultadoActivity"
            android:label="@string/title_activity_resultado"
            android:theme="@style/Theme.Pos2020.NoActionBar" /> <!-- <activity -->
        <!-- android:name=".CicloVidaActivity" -->
        <!-- android:label="@string/title_activity_ciclo_vida" -->
        <!-- android:theme="@style/Theme.Pos2020.NoActionBar"></activity> -->
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Pos2020.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>